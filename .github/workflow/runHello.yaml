name: Run first hello welcome to yaml

on: 
  push:
    branches:
      - main

jobs:
  print-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello from first yaml file"


on: 
  workflow_dispatch: 



jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: "echo  Hello from ${{ github.job }}"

  job-2:
    runs-on: ubuntu-latest
    needs: job-1
    steps:
      - name: Print Hello
        run: "echo  Hello from ${{ github.job }}"

  job-3:
    runs-on: ubuntu-latest
    needs: job-2
    steps:
      - name: Print Hello
        run: "echo  Hello from ${{ github.job }}"

  job-4:
    runs-on: ubuntu-latest
    needs: job-2
    steps:
      - name: Print Hello
        run: "echo  Hello from ${{ github.job }}"
        
